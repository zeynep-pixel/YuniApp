specVersion: "v1beta"
serviceId: "yuapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "yuapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
